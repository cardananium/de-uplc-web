[package]
name = "de-uplc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
uplc = { git = "https://github.com/cardananium/aiken", rev = "6b8da868d514f40cb855fad6945a613bab2d5d0b" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
pallas-primitives = "0.35"
pallas-codec = { version = "0.35", features = ["num-bigint"] }
pallas-traverse = "0.35"
pallas-addresses = "0.35"
pallas-crypto = "0.35"
blst = "0.3"
num-bigint = "0.4"
schemars = "1.0.4"
thiserror = "2.0.10"
getrandom = { version = "0.2.16", features = ["js"] }

# Target-specific dependencies
[target.'cfg(all(target_arch = "wasm32", not(target_os = "emscripten")))'.dependencies]
wasm-bindgen = { version = "0.2.100", features = ["serde-serialize"] }
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console"] }
console_error_panic_hook = "0.1"

[target.'cfg(not(all(target_arch = "wasm32", not(target_os = "emscripten"))))'.dependencies]
noop_proc_macro = "0.3"
serde_derive = "1.0"
actix-web = "4.9"
actix-cors = "0.7"
tokio = { version = "1.0", features = ["full"] }
env_logger = "0.11"
tokio-util = "0.7"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = ["-O4", "--enable-bulk-memory"]