# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# Rust build artifacts (engine crate; also has its own rust-src/.gitignore)
rust-src/target/
rust-src/pkg/

_disabled/
apps/web/e2e/

# OS / editor
.DS_Store

# Generated Rust→WASM engine output — NO LONGER committed. CI builds it before the web
# build (.github/workflows/ci.yml); locally the prebuild guard (scripts/ensure-wasm.mjs)
# builds it on demand. Rebuild manually with `npm run build:wasm` (needs Rust + wasm-pack).
packages/engine-wasm/pkg/
